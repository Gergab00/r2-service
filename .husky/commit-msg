#!/usr/bin/env sh

# Ruta: /.husky/commit-msg
#
# Propósito:
# Validar automáticamente el mensaje del commit antes de confirmarlo.
#
# Responsabilidad única:
# Ejecutar commitlint sobre el mensaje recibido por Git.

pnpm exec commitlint --edit "$1"
